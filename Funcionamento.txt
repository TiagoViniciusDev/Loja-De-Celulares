Como funciona?

//usa o useContext para fazer comunicação entre os componentes

//Consumo de dados da api
- Feito usando um fetch que recebe um array como resposta
- Passamos os dados recebido para um useState que gurda nossos dados
- um map percorre esse array, mandando um a um esses dados para um componente que será executado várias vezes até o ultimo item 

//Adionar itens ao carrinho:
- É feita uma comunição entre o componente que exibe os produtos e o componente carrinho como o useContext
- O componente do produto passa os dados do item para o componente carrinho caso haja o clique para adicionar o produto. 

